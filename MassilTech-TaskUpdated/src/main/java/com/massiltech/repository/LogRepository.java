/*
 * package com.massiltech.repository;
 * 
 * import org.springframework.data.jpa.repository.JpaRepository; import
 * org.springframework.stereotype.Repository;
 * 
 * import com.massiltech.entity.Log; import com.massiltech.entity.Patient;
 * 
 * @Repository public interface LogRepository extends JpaRepository<Log,
 * Integer> {
 * 
 * //Log findByPatientId(Patient id); //Log findByPatientId(Integer id);
 * 
 * }
 */